<!DOCTYPE html>
<html>
<head>
  <link href="https://fonts.googleapis.com/css?family=Baloo|Baloo+Thambi|BioRhyme|Hind|Indie+Flower|Poiret+One" rel="stylesheet">

  <title>Blocipedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="container">
    <div class="navbar">
      <ul class="nav nav-tabs">
        <li id="hind"><%= link_to "Home", root_path %></li>
        <li id="hind"><%= link_to "About", about_path %></li>
        <div class="pull-right user-info">
          <% if user_signed_in? %>
            <p id="hind">Signed in as: <strong><%= link_to current_user.username, user_path(current_user) %></strong> |
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></p>
          <% else %>
          <p id="hind">
            <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link'  %> -
            <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %>
          </p>
          <% end %>
        </div>
      </ul>
      <% flash.each do |key, value| %>
        <div class="alert <%= alert_class_for(key) %> alert-dismissible fade in">
          <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
          <ul>
            <li>
              <%= value %>
            </li>
          </ul>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </div>

</body>
</html>
